/*#include<stdio.h>
#include<string.h>
#define MAX 100

int a(int* arr, int n) {
	int* p = arr;
	for (int i = 0; i < n; i++) {
		printf("%d ", *(p + i));
	}
}

int b(int* arr, int n) {
	for (int i = 0; i < n; i++) {
		*(arr + i) = 1;
		printf("%d", &arr);
		return 0;
	}
}


int main(void) {
	int arr[MAX];
	printf("input:");
	scanf_s("%99
}*/