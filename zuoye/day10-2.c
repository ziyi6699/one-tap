/*#include <stdio.h>
#include <string.h>
int main() {
	char str1[100];
	char str2[100];
	
	
	printf("请输入一行字符串:\n ");
	scanf_s("%[^\n]", str1, (unsigned int)sizeof(str1));
	getchar();
	printf("请输入一行字符串:\n ");
	scanf_s("%[^\n]", str2, (unsigned int)sizeof(str2));
	getchar();


	if (strcmp(str1, str2) == 0) {
		printf("两个字符串相等\n");
	} else {
		printf("两个字符串不相等\n");
	}

	if (strcmp(str1, str2) != 0) {
		int long_1, long_2;
		long_1 = strlen(str1);
		long_2 = strlen(str2);
		printf("第一个字符串长度为:%d", long_1);
		printf("\n第二个字符串长度为:%d", long_2);
	}else{
		return 0;
	}
	return 0;
	}*/

	
	
	

