/*#include <stdio.h>
#include <stdlib.h>

int isleapyear(int year) {
    return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
}

int main(void) {
    int year;
    printf("输入年份: ");
    scanf_s("%d", &year);
    

    if (isleapyear(year)) {
        printf("%d 是闰年\n", year);
    }
    else {
        printf("%d 不是闰年.\n", year);
    }
    return 0;
}*/
	
