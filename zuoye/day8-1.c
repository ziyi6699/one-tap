/*#include<stdio.h>
int main() {
	int score[5];
	for (int i = 0; i < 5; i++) {
		printf("输入成绩 %d: ", i + 1);
		scanf_s("%d", &score[i]);
	}
	double ave = 0;
	for (int i = 0; i < 5; i++) {
		ave += score[i];
	}
	ave /= 5;
	printf("平均分: %.2f\n", ave);
	double max = score[0];
	for (int i = 1; i <= 5; i++) {
		if (score[i] > max) {
			max = score[i];
		}
	}
	printf("最高分: %.2f\n", max);
	return 0;
}/*/
